# Dependências do servidor MCP gabaritos_mcp (além do requirements.txt da API)
# Instalação: uv pip install --python .venv/bin/python -r mcp_gabaritos/requirements-mcp.txt
fastmcp>=2.0
httpx>=0.27
pydantic>=2.0
